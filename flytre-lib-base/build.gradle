archivesBaseName = "flytre-lib-base"
version = getSubprojectVersion(project, "1.3.0")

loom {
    accessWidenerPath = file("src/main/resources/flytre-lib-base.accessWidener")
}

moduleDependencies(project, [])

dependencies {
}